export function analyzeBookText(args:{title?:string;text:string}){const text=args.text||"";if(text.length<200)throw new Error("book text too short");const lines=text.split(/\r?\n/);const headings=lines.filter(x=>/^#{1,4}\s+/.test(x)||/^(chapter|part|الفصل|الباب)\s+/i.test(x.trim())).slice(0,200);const tokens=text.toLowerCase().match(/[\p{L}\p{N}_-]{4,}/gu)||[];const stop=new Set(["that","this","with","from","have","will","your","into","about","there","their","which","على","هذا","هذه","التي","الذي","الى","إلى","كان","كما"]);const freq=new Map<string,number>();for(const t of tokens)if(!stop.has(t))freq.set(t,(freq.get(t)||0)+1);const concepts=[...freq.entries()].sort((a,b)=>b[1]-a[1]).slice(0,50).map(([term,count])=>({term,count}));return{title:args.title||"Untitled",characters:text.length,headings,concepts,note:"Concept frequency is a discovery aid, not semantic proof. Persist source citations separately."};}
