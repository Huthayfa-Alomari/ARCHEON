export type EvidenceRow={design?:string;sampleSize?:number;randomized?:boolean;blinded?:boolean;peerReviewed?:boolean;riskOfBias?: "low"|"some-concerns"|"high";directness?: "direct"|"indirect";precision?: "precise"|"imprecise"};
export function appraiseEvidence(args:{studies:EvidenceRow[]}){const s=args.studies||[];if(!s.length)throw new Error("studies required");const rows=s.map((x,i)=>{let score=0;const d=(x.design||"").toLowerCase();if(/systematic|meta/.test(d))score+=4;else if(/random/.test(d)||x.randomized)score+=3;else if(/cohort|case-control/.test(d))score+=2;else score+=1;if((x.sampleSize||0)>=1000)score+=2;else if((x.sampleSize||0)>=100)score+=1;if(x.blinded)score+=1;if(x.peerReviewed)score+=1;if(x.riskOfBias==="high")score-=3;else if(x.riskOfBias==="some-concerns")score-=1;if(x.directness==="indirect")score-=1;if(x.precision==="imprecise")score-=1;return{index:i,score,quality:score>=7?"higher":score>=4?"moderate":"lower",gaps:Object.entries({design:!x.design,sampleSize:!x.sampleSize,riskOfBias:!x.riskOfBias,directness:!x.directness,precision:!x.precision}).filter(([,v])=>v).map(([k])=>k)}});return{studies:rows,summary:{higher:rows.filter(x=>x.quality==="higher").length,moderate:rows.filter(x=>x.quality==="moderate").length,lower:rows.filter(x=>x.quality==="lower").length},limitation:"Heuristic evidence appraisal only. It does not replace a formal GRADE or risk-of-bias assessment and must not be used as autonomous clinical advice."};}
