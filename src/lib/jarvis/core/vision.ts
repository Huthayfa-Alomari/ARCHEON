export function visionTask(args:{source:"screenshot"|"phone-camera"|"meta-camera"|"file";goal:string}){if(!args.goal?.trim())throw new Error("goal required");return{source:args.source,goal:args.goal,pipeline:["permission check","capture/read image","vision model analysis","structured observations","optional action proposal","owner approval for actions"],rule:"Image observations are evidence inputs; actions never inherit capture permission automatically."};}
