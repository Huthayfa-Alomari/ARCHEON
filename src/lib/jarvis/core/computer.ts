import { execFile } from "node:child_process";import { promisify } from "node:util";import fs from "node:fs/promises";import path from "node:path";const exec=promisify(execFile);
export async function computerStatus(){return{platform:process.platform,screenshot:process.platform==="win32",osActionsEnabled:(process.env.JARVIS_ENABLE_OS_ACTIONS||"").toLowerCase()==="true",rule:"Screen capture and machine actions require explicit approval; no unrestricted shell is exposed."};}
export async function captureScreenshot(){if(process.platform!=="win32")throw new Error("Screenshot adapter currently supports Windows only.");const dir=path.resolve("./data/screenshots");await fs.mkdir(dir,{recursive:true});const file=path.join(dir,`screen-${Date.now()}.png`);const script=`Add-Type -AssemblyName System.Windows.Forms; Add-Type -AssemblyName System.Drawing; $b=[System.Windows.Forms.Screen]::PrimaryScreen.Bounds; $bmp=New-Object System.Drawing.Bitmap $b.Width,$b.Height; $g=[System.Drawing.Graphics]::FromImage($bmp); $g.CopyFromScreen($b.Location,[System.Drawing.Point]::Empty,$b.Size); $bmp.Save('${file.replace(/'/g,"''")}',[System.Drawing.Imaging.ImageFormat]::Png); $g.Dispose(); $bmp.Dispose()`;await exec("powershell.exe",["-NoProfile","-NonInteractive","-Command",script],{timeout:15000,windowsHide:true});return{path:file};}
